jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Use API Key
        env:
          OPENAI_API_KEY: ${{ secrets.API_KEY }}
        run: |
          # Your commands that use the API key here